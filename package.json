{
  "name": "biddingflow-frontend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test controllers/**/*.test.mjs views/**/*.test.mjs",
    "check": "npm test && npm run build",
    "build": "vite build",
    "build:secure": "vite build --mode secure"
  },
  "devDependencies": {
    "javascript-obfuscator": "^5.4.3",
    "vite": "^4.5.0"
  }
}
