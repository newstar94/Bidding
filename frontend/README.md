# Frontend source root

Mã JavaScript ứng dụng được di chuyển tuần tự vào thư mục này theo `docs/source-structure-plan.md`.

`views` tiếp tục chứa HTML, CSS và tài nguyên public; không đặt workflow hoặc view JavaScript mới trong `views`.
