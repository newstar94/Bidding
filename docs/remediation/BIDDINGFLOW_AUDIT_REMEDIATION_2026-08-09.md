# BiddingFlow audit remediation — 2026-08-09

Source audit: `docs/audit/BAO_CAO_AUDIT_TOAN_DIEN_BIDDINGFLOW_2026-08-09.md`

## Baseline

| Item | Current-SHA baseline |
|---|---|
| HEAD / origin | `47629c307b1ab6289918eb638fdddf09488a7639` / same; ahead/behind `0/0` after `git fetch origin` |
| Python | 633/633 pass; current `.coverage` and `coverage.json` generated |
| Coverage | combined 44.974%; statements 48.663%; branches 35.0%; global gate and critical-module gate pass |
| JavaScript | 522/522 pass; no skip/fail/todo |
| Frontend graph/debt | 258 modules, 0 cycles; direct writes 59, `!important` 421, inferred actions 6, raw colors 842, runtime styles 541 |
| Python quality | BLE001 118, S608 128, F401/F841/S110 0; gate pass |
| Security/encoding/vendor | ESLint + Trusted Types, mojibake guard, 8 pinned vendor files, SheetJS/archive guards pass |
| Database | PostgreSQL 17.10, schema v42, 79 tables, 379 indexes, 104 FKs; FK index audit 104/104 |
| Secure build | 262 modules; 46 obfuscated bundles; pass |
| Dependencies | `npm audit --omit=dev` pass; `pip-audit` pass; full `npm audit` fails one HIGH `nanoid@3.3.16` |
| SBOM | baseline generator completes, but audit BF-P2-17 identifies missing vendored components |
| Production package | blocked before build/package by `LEGAL_FACT_UNAPPROVED=27`, `LEGAL_PLACEHOLDER_PRESENT=27`; gate not bypassed |
| Performance | cold p95 539 ms, warm p95 150 ms; cold longest task 145 ms > 100 ms, overall baseline fail |
| E2E baseline | Source audit §16 records auth/UI/RBAC/CRUD/multi-assignee/JV/bidder-goods/low-price/offline/pairwise/lifecycle evidence; offline was flaky once then passed twice; canonical three-browser contractor-risk spec skipped for missing fixture conditions |

## Status vocabulary

Final status must be exactly one of:

- `FIXED`
- `MITIGATED`
- `VERIFIED RESOLVED ON LATEST HEAD`
- `BLOCKED BY EXTERNAL/LEGAL/PRODUCT DECISION`
- `DEFERRED BY AUDIT SAFETY RULE`

`PENDING` is used only while implementation is in progress and is not an acceptable final disposition.

## Master remediation status

| ID | Before | Repro/Test | Root cause | Fix | Commit | Tests | Migration | Status | Remaining risk |
|---|---|---|---|---|---|---|---|---|---|
| BF-P0-01 | P0; CONFIRMED BUG / TENANT ISOLATION; Frontend async reads | Deferred A→B reproduced ở pagination, plan hydration, record lookup, init/admin users, notifications, package/contract employees, Word templates/mappings và timeline record fetches | Async loaders dereference `model.state`/`model.db` và render DOM sau `await` mà không có workspace capability; transition chỉ reset hai request maps cục bộ | Workspace lease capture token/scope/state/DB; model-wide AbortController registry; validate trước mọi state/IDB/DOM/cache effect; transition abort/clear toàn bộ loader request | `07a85ba7`, `7aa9863` | `plan_package_snapshot_hydration`, `workspace_loader_lease`, `workspace_request_lifecycle`; full JS 535/535; security/module/debt/encoding pass | N/A | FIXED | Không còn known stale-read path trong các loader được audit; loader mới phải dùng workspace lease |
| BF-P0-02 | P0; CONFIRMED BUG / DATA INTEGRITY; Frontend mutation | Deferred A→B reproduced cho add/update/delete, full-table persist, mixed upsert+delete, multi-table mutation, validation await và durable-failure rollback | Mutation dereference mutable `state`/`db`/outbox sau `await`; direct/high-level APIs không cùng một logical capability; transition đóng/đổi DB mà không drain | Mutation capability capture token/scope/state/DB/storage/outbox; nested persistence reuse capability; `WorkspaceDataStore` giữ capability qua validation+rollback; transition khóa write mới và drain trước close/swap | `90105cd` | `workspace_mutation_lease` 10 cases; full JS 545/545; security/module/debt/encoding pass | N/A | FIXED | Không còn known A→B mutation path trong seams audit; rollback atomic cho legacy direct failure vẫn được xử lý riêng ở BF-P1-03 |
| BF-P0-03 | P0; CONFIRMED BUG / SECURITY; AI RBAC | Matrix reproduced: manager A/employee B, employee A/manager B, no-membership B, demotion, org switch và stale selected role; manager A trước fix tạo full-scope SQL ở B | `active_role` session-global không có workspace provenance; AI `manager_context` và query/search restriction tin role cũ thay vì membership live của org hiện tại | Bind role selection với workspace; ignore unbound/mismatched role; rederive effective AI role từ current membership trên mọi request/tool execution; bootstrap cũng rederive | `a9ee7b3`, `45241db` | `test_ai_permission_context`, `test_session_request_context`, `test_session_active_role_scope`; full Python 648/648; negative analytics + workspace-search SQL assertions pass | v43 `bind_session_active_role_to_workspace`; nullable additive column + compatibility CHECK; rollback binary cũ được hỗ trợ, unbound role luôn bị app mới bỏ qua | FIXED | Không còn known cross-org AI manager path; no-membership trả `AI_PERMISSION_DENIED` |
| BF-P1-01 | P1; CONFIRMED BUG; Pull/outbox merge | Reproduced delta/full server stale overwrite, server delete vs local upsert, reference/manifest resurrection, empty reconnect delta và second edit field loss | Outbox snapshot được đọc sau incoming merge; protection chỉ dùng cho reference/manifest và chỉ pending upsert, không pending delete | Snapshot durable queue trước merge; pending upsert/delete là visible + IndexedDB overlay cho delta/full/reference/manifest/server-delete; tách overlay deletion khỏi server acknowledgement | `e61bee7` | `sync_pending_overlay` 9 cases; full JS 554/554; security/module/debt/encoding pass; debt ratchet giữ 50 | N/A | FIXED | Pending payload vẫn whole-record theo domain contract nhưng không còn được xây từ server-stale state |
| BF-P1-02 | P1; CONFIRMED BUG / RACE; Pull ordering | Reproduced older pull completing after newer pull for route/background and WS/manual; before fix final state and durable cursor could regress | Concurrent pulls had no workspace generation/serialization boundary; every successful response could apply state, persist, commit cursor, render and overwrite UI status | Per-workspace generation guard before apply and after persistence; superseded completion/error/finally cannot commit cursor, render, regress status or hide the active loader; workspace transition clears generations; 409 full-sync recursion owns the new generation | `df09771` | `sync_pull_ordering` 3 cases; full JS 557/557; security/module/debt/encoding pass; debt ratchet giữ 50 | N/A | FIXED | Persistence begins only after latest-generation validation; IndexedDB transaction ordering plus post-persistence guard prevents stale cursor/render publication |
| BF-P1-03 | P1; CONFIRMED BUG; Local persistence | Reproduced add/update/delete failures after in-memory mutation, localStorage outbox staging failure, IndexedDB outbox failure and fire-and-forget evaluation metadata rejection; reload returned the old durable row | Legacy record APIs mutated state before entity persistence, did not checkpoint/flush outbox, and had no compensation; timeline/custom-status/evaluation callers also mutated canonical state before entering the API | Record-scoped state/outbox checkpoint; wait for entity + durable outbox; compensate IndexedDB and restore exact outbox on any failure; remove pre-API canonical writes; evaluation caller stages a detached record and reports/rerenders on failure | `5501240` | `legacy_record_atomicity` 5 cases + `bid_evaluation_metadata_persistence`; full JS 563/563; security/module/debt/encoding pass; debt ratchet giữ 50 | N/A | FIXED | Compatibility API now compensates instead of claiming a cross-store ACID transaction; rollback errors are attached for diagnostics and the original failure remains authoritative |
| BF-P1-04 | P1; DATA INTEGRITY RISK; Dual outbox | Reproduced localStorage-only, IndexedDB-only, both-read, write, malformed JSON, invalid envelope and divergent same-revision failures; old code either skipped the second backend or normalized uncertainty to an empty queue | Backend reads/writes were independent best-effort operations without health/trust state; local write threw before IndexedDB scheduling; read/corruption exceptions were swallowed; pull/push never checked outbox durability | Always attempt both configured backends; strict envelope validation; monotonic newest-replica repair; explicit pending/ready/degraded + per-backend status; manual recovery for corruption; block synced edits/push/pull while untrusted and wait pending writes before network | `27644ae` | `outbox_durability` 11 cases covering localStorage/IndexedDB/both/corruption/conflict/recovery/write/pending/pull; full JS 574/574; security/module/debt/encoding pass; debt ratchet giữ 50 | N/A | FIXED | Same-revision divergent valid replicas remain fail-closed for explicit reconciliation; neither copy is overwritten automatically |
| BF-P1-05 | P1; CORRECTNESS RISK; Contractor versioning | Reproduced independent and JV lookup enrichment against the contractor version effective on opening date; name/address/email/type changes rewrote existing historical/latest rows and restaged them | Opening collector treated tax lookup as master-data enrichment and mutated the selected contractor object; read resolvers then preferred mutable contractor fields over the bid/JV-member snapshot | Existing contractor versions are read-only; only genuinely new contractors enter `changedContractors`; opening lookup values are frozen on existing `thong_tin_mo_thau` and JV-member snapshot fields; resolvers prefer snapshot values after reload | `ae97330` | `contractor_opening_snapshot` 2 business-date/history/reload cases; opening/award targeted 27/27; full JS 576/576; security/module/debt/encoding pass | N/A | FIXED | Independent snapshot contract stores canonical opening name/type/code; detailed contact enrichment is available on JV-member snapshots, while master-data corrections require the dedicated contractor-version workflow |
| BF-P1-06 | P1; BUILD/DEPLOY DEFECT; Production package | Path-contract regression reproduced four README references absent from the archive: two security/load scripts, production information sheet and DDoS runbook | Runtime allowlist omitted the operational scripts while source-only `docs/` paths were forbidden from production selection | Add both scripts to the explicit runtime allowlist; ship package-specific checklist/runbook under `deploy/`; rewrite README references; scan inline and command-block operational paths against `collect_runtime_files()` | `ef5886b` | Red path-contract reported 4 missing paths; `test_production_package` 4/4; full Python 649/649; coverage 45.15% + critical 15-module gate; extracted package smoke 388 files | N/A | FIXED | Legal approval remains an intentional independent package blocker tracked by BF-P1-07 |
| BF-P1-07 | P1; INTENTIONAL RELEASE BLOCKER; Legal/CI | Revalidated 27/27 fact-sheet rows as `missing` with no evidence, approval date or approver; all 27 corresponding public placeholders remain present | The repository intentionally lacks authoritative operator/legal facts; production gate correctly distinguishes warning-only development from fail-closed public packaging | Preserve the gate unchanged; require authorized owners to provide evidence, approve each fact and replace reviewed public copy before packaging | N/A — external approval required | `npm run check:legal` exits 0 with `LEGAL_READINESS_WARNING`; `npm run check:legal:production` exits 1 with `LEGAL_READINESS_BLOCKED`; both report `LEGAL_FACT_UNAPPROVED=27`, `LEGAL_PLACEHOLDER_PRESENT=27` | N/A | BLOCKED BY EXTERNAL/LEGAL/PRODUCT DECISION | Production public artifact remains intentionally blocked; no approval was inferred or fabricated |
| BF-P1-08 | P1; CONFIRMED BUG / SECURITY; Word access policy | Red regression reproduced seven partial/deny combinations resolving to all-true and organization A deny being overwritten by organization B allow; stored `0/0/0` did not redact DOCX context | Compatibility projection deliberately ignored `document_export_capabilities` for every active member even though admin UI/API persisted and presented the flags as deny gates | After entitlement/record authorization, managers and personal owners inherit all; ordinary members resolve the exact organization-scoped stored row, fail closed when absent, and existing DOCX DTO/mapping projection removes denied financial, identity and signature fields | `d564783` | 8 exhaustive grant combinations with DOCX negative assertions + manager inheritance + two-org isolation; Word/export targeted 40 pass; full Python 659/659; coverage 45.30% + critical gate; Python quality pass | N/A | FIXED | Application record reads remain governed by record policy and are not masked; only Word export field families use these grants, matching the current UI contract |
| BF-P1-09 | P1; CONFIRMED BUG / RACE; Delete concurrency | Real PostgreSQL two-connection barriers reproduced hard-delete and referenced-record archive after v1 prefetch while a second transaction committed v2; old service reported success and created delete impact | Optimistic version check happened only against the prefetched dictionary; final `DELETE`/archive `UPDATE` matched identity and archive state but not `row_version`, and dependent cleanup preceded the unchecked mutation | Conditional hard delete and archive on captured `row_version`; require rowcount exactly 1; wrap assignment/participant cleanup and root mutation in a savepoint; rollback on CAS loss, reload authorized current row and return `ROW_VERSION_CONFLICT` without tombstone/audit; preserve missing/already-archived retry no-op | `9cf0d48` | 2 real PostgreSQL barrier cases for hard-delete + archive preserve v2/currentVersion=2/no tombstone; delete/opening/N+1 targeted 27 pass; full Python 661/661; coverage 45.34% + critical gate; Python quality pass | N/A | FIXED | CAS protects root-row updates; callers must continue incrementing root `row_version` for business changes, while concurrent completed delete/archive remains idempotent |
| BF-P1-10 | P1; CONCURRENCY RISK; Member quota | Real PostgreSQL last-slot interleaving reproduced two transactions reading `current=1/quota=2` and committing two activations, ending with 3 active members; reproduced for new insert and reactivation | Subscription and active-member count were ordinary reads; no shared row lock serialized the count with insert/reactivate, so both transactions consumed the same logical slot | `_lock_organization_member_quota` locks the organization subscription row, then recounts active memberships inside the caller transaction; add/reactivate uses that state immediately before quota decision and write, sharing the same subscription lock used by package/quota updates | `7d2971b` | 2 real PostgreSQL last-slot cases: insert-vs-insert and reactivate-vs-insert, exactly one succeeds; org/N+1 targeted 30 pass; full Python 663/663; coverage 45.36% + critical gate; Python quality pass | N/A | FIXED | All code-owned add/reactivate paths now serialize on the subscription row; future membership writers must reuse the same invariant seam |
| BF-P1-11 | P1; CONCURRENCY RISK; Last manager | Real PostgreSQL interleavings reproduced remove-vs-demote and demote-vs-remove: both transactions read two active managers and committed, leaving zero | Last-manager checks in member removal, role update, Super Admin access settings and account deletion had no shared serialization boundary; multi-organization account deletion also lacked deterministic lock ordering | Canonical `to_chuc ... FOR UPDATE` membership-invariant lock; apply before every code-owned manager-loss path; account deletion locks all affected organizations in sorted order and rechecks active manager state; subscription mutation now follows `organization → subscription` lock order | `10bf594` | 2 real PostgreSQL remove/demote races preserve exactly one manager + deterministic multi-org lock-order case; auth/org/security targeted 35 pass; full Python 666/666; coverage 45.38% + critical gate; Python quality pass | N/A | FIXED | Current membership writers share the invariant seam; any future promotion/demotion/removal/account-deletion path must acquire the same organization lock |
| BF-P1-12 | P1; CONCURRENCY RISK; Last super-admin | Isolated real PostgreSQL interleavings reproduced delete-vs-demote and demote-vs-delete for the final two super-admins; both transactions read count 2 and committed, leaving zero | Platform-role count and mutation had no shared singleton row or advisory boundary across role API, Super Admin access settings and account deletion | Transaction-scoped named PostgreSQL advisory lock acquired before platform role read/count/write in all three loss paths; multi-invariant operations follow `platform → organization → subscription` ordering | `9dee5fc` | 2 real PostgreSQL final-two-super-admin delete/demote cases preserve exactly one; admin/auth/manager targeted 36 pass; full Python 668/668; coverage 45.38% + critical gate; Python quality pass | N/A | FIXED | The invariant relies on all future super-admin loss paths reusing the named advisory lock; promotions need not lock unless combined with another platform-role mutation |
| BF-P1-13 | P1; CONCURRENCY RISK; Package documents | Real PostgreSQL interleaving reproduced package status changing from upload-allowed to `CANCELLED` after final read but before document upsert/delete; both stale mutations committed | Upload revalidated after file staging and delete checked once inside its transaction, but neither final lifecycle check held a package/batch lock through the document write | Dedicated package mutation loader uses `SELECT ... FOR UPDATE`; batch-scoped mutation validation also locks the evaluation batch; final upload/replace/delete routes hold those rows from authorization/lifecycle validation through document DB mutation and commit | `b0f4f66` | 2 real PostgreSQL lifecycle-vs-document cases for upload + delete prove transition blocks until document commit; documents/routes/schema targeted 12 pass; full Python 670/670; coverage 45.40% + critical gate; Python quality pass | N/A | FIXED | File bytes are staged before the DB lock to keep lock duration bounded; a failed final revalidation removes the staged key, while old-file cleanup remains post-commit best effort |
| BF-P1-14 | P1; CONFIRMED BUG; Personal realtime sync | Two independent personal-workspace tabs reproduced the same failure: `onopen` stopped polling before authentication, then backend 4003 entered the permanent-close branch without restoring it, leaving neither WS nor polling | Transport-open was treated as an authenticated usable channel; there was no server readiness acknowledgement, non-retryable close returned before fallback recovery, and stale socket handlers were not explicitly isolated from a new workspace socket | Start polling during CONNECTING/authentication; backend sends an organization-bound `ready` frame only after session, workspace and cluster lease validation; stop fallback only for the matching current socket/workspace; current permanent close retains polling, while logout stops it and stale old-socket close is ignored | `d1d037e` | Red JS regression failed 3/6 before fix; WebSocket lifecycle targeted 6/6 + backend ready contract 1/1; full Python 671/671, coverage 45.72% + critical gate; full JS 579/579; quality/security/modules/debt/encoding pass | N/A | FIXED | Personal pseudo-workspaces remain polling-only while online because they are not ID-backed organization memberships; each tab owns an independent 30-second fallback timer, so maximum realtime latency is bounded rather than stale indefinitely |
| BF-P2-01 | P2; CONFIRMED DATA-FIDELITY DEFECT; Generated schema | Reproduced the 896-character backend-to-browser drift; package appraisal `yeuCauThamDinhHsmtCode` was reported unknown and omitted from outbound payloads, while export-capability, normalized-lot and violation mappings were absent from the runtime artifact | The generator was manually invoked, had no deterministic render/check API, and was absent from package/CI gates, so later backend schema additions never refreshed the committed browser contract | Regenerate all mappings from `SCHEMA_DINH_NGHIA`; expose deterministic render and non-mutating `--check`; add npm generate/check commands, local quality gate and explicit CI drift step; lock appraisal serialization with a public serializer contract test | `dcbb9cd` | Red artifact import/check and serializer regression; targeted Python 1/1 + JS 1/1; full Python 672/672, coverage 45.72% + critical gate; full JS 580/580; secure build 264 modules/47 bundles; quality/security/modules/debt/encoding pass | N/A | FIXED | Newly built clients preserve the canonical appraisal enum and inbound mappings; returning-client bootstrap cache invalidation is tracked independently by BF-P2-02 |
| BF-P2-02 | P2; CONFIRMED CACHE DEFECT; Bootstrap assets | Reproduced one-year immutable caching for any static `?v=` value, including manual `v=2.0`; `route-shell`, `initial-route` and `lucide-shim` could change while returning clients kept the old response, and the initial-route preload duplicated the stale version literal | Cache policy treated presence of an arbitrary query version as proof of content addressing; direct bootstrap scripts had hand-maintained versions and secure build had no source-to-URL digest assertion | Generate cross-platform-stable SHA-256 URLs for all three direct bootstrap scripts; secure build runs a non-mutating drift check; derive initial-route preload from the compiled hashed script URL; grant immutable caching only to Vite `/dist/assets/` or a full 64-hex version, while manual query versions revalidate | `18b2672` | Red cache-policy + preload/source-hash regressions; bootstrap/startup/legal targeted 17/17; full Python 675/675, coverage 45.75% + critical gate; full JS 580/580; secure build 264 modules/47 bundles; quality/security/modules/debt/encoding pass | N/A | FIXED | Production JS/CSS bundle remains Vite content-hashed; direct CSS and development ESM manual versions now use conditional revalidation rather than long-lived immutable caching |
| BF-P2-03 | P2; CORRECTNESS/COMPAT RISK; Contractor selection | Reproduced a business date before the first partner version selecting the oldest future version; opening auto-lookup and contract signing/liquidation then silently bound that not-yet-effective row | Date resolver always returned a record: after finding no applicable version it sorted ascending and selected the first future row; opening also treated an auto lookup ID like a frozen saved binding, and contract callers fell back to the originally selected future ID | Add structured `matched`/`no_effective_version` resolution; selector returns null before first effective date; distinguish saved opening binding from automatic lookup; fail opening before state/master mutation with actionable feedback; block automatic contract date bindings while preserving explicit saved/manual selections | `adaad19` | Red pre-effective resolver + opening no-mutation + actionable-save regressions; partner/opening/contract/JV targeted 32/32; full Python 675/675, coverage 45.75% + critical gate; full JS 583/583; security/modules/debt/encoding pass | N/A | FIXED | Legacy families with no parseable effective/created/updated date retain the selected row under explicit `effective_date_unavailable` compatibility status; dated families can no longer select future data implicitly |
| BF-P2-04 | P2; MEMORY/PRIVACY RISK; JV render cache | Reproduced identical package/JV keys resolving from one process-global object regardless of workspace; neither workspace reset nor rerender released the retained contractor/JV member and lot-winner references | JV and lot-winner stores were mutable module singletons with no workspace identity, owner lifecycle, reset hook or cardinality bound; controller actions dereferenced them by record ID alone | Introduce an explicit workspace-scoped shared render cache; isolate reads/writes by `workspaceScope.key`; mark package list, award, evaluation and modal entries by render owner; dispose owner entries on replacement render and all entries on workspace reset; enforce 256 entries/workspace plus four-workspace LRU hard caps | `e085679` | Red A/B isolation, owner replacement, 257-entry eviction, workspace reset and registry-cap regressions; targeted 5/5; full JS 588/588; security/Trusted Types, modules 261/0 cycles, debt and encoding pass | N/A | FIXED | Render-only data now cannot cross a workspace boundary and cannot grow without bound; modal entries live only until replacement render or workspace reset, while authoritative data remains in workspace state |
| BF-P2-05 | P2; UX/ACCESSIBILITY ISSUE; Custom select | Browser regression found no named `combobox` after generic or searchable initialization; hidden native selects were replaced by focusless div/input/list markup with click handlers but no listbox/options, expanded state, active descendant or keyboard selection contract | Generic and partner helpers independently constructed legacy controls instead of reusing the existing accessible combobox; Assistant then added a partial button/listbox patch only for one instance | Route both public helpers through canonical `initAccessibleCombobox`; preserve version/table/searchable compatibility classes and portal geometry; add label/described/error/disabled propagation, mutation/reset observer, ArrowUp/Down, Home/End, Enter, Space and Escape behavior, active descendant, selected option state and lifecycle cleanup; delete both click-only implementations | `3af8dc6` | Red Chromium keyboard/accessibility regressions for generic + searchable controls; axe form smoke; compatibility targeted 6/6 for inline/version/Assistant; full JS 590/590; secure build 265 modules/48 bundles; security/modules/encoding pass; runtime-style debt reduced 541→493 | N/A | FIXED | Canonical controls are keyboard and accessibility-tree operable in Chromium; enforced Firefox/WebKit coverage remains tracked by BF-P2-14 rather than inferred here |
| BF-P2-06 | P2; CONFIRMED ACCESSIBILITY ISSUE; Package detail 320px | Reproduced disabled custom-select text at 2.05:1 after accounting for the ancestor opacity group on the current canonical input; automated axe alone returned no violation because disabled native controls are excluded, matching the audit gap that originally measured 3.86:1 on legacy span text | Two duplicate disabled rules combined muted foreground/background with container opacity 0.6/0.7, blending both toward the page backdrop and destroying effective contrast | Remove group opacity; use reviewed `--text-main` on `--neutral-soft` with semantic border token in both compatibility rules; add 320px Chromium regression using axe plus an independent WCAG relative-luminance calculation that includes ancestor opacity | `1ca246b` | Red focused regression measured 2.05:1; fixed token pair measures 15.86:1; 320px axe+WCAG targeted 3/3; full JS 591/591; secure build 265 modules/48 bundles; security/modules/encoding pass; raw-color debt reduced 842→836 | N/A | FIXED | Disabled text now exceeds 4.5:1 without weakening disabled behavior; authenticated full-route visual matrix will be rerun in final acceptance, while Firefox/WebKit enforcement remains BF-P2-14 |
| BF-P2-07 | P2; DUPLICATED LOGIC / RISK; Lot JSON parsing | Reproduced malformed and parsed-non-array lot values silently becoming `[]` in six helpers while five render/command paths threw raw `JSON.parse` errors; a legacy award command could therefore fail open in low-price lookup before failing later | Lot storage decoding, absence semantics, error classification and recovery policy were reimplemented per caller; tolerant catches had no operational signal, while unchecked display parses could break the whole render | Add one canonical parser with typed strict command mode and fail-safe display mode; bound error/context/input-kind cardinality; exclude raw lot/private data; report display recovery through rate-limited release diagnostics; migrate award, evaluation, price, table, form, panel, history and view-model callers; validate legacy award JSON before package mutation | `ac8589e` | Red parser vectors for array/blank/valid/malformed/non-array/type, single recovery event, context bounding and no-payload assertions; command no-mutation regression; full JS 597/597; security/Trusted Types, encoding and debt pass; modules 262/0 cycles; secure build 266 modules/48 bundles | N/A | FIXED | JSON container corruption is now deterministic and observable without exposing contents; individual lot object shape remains validated by the owning evaluation/award workflow rather than being silently rewritten by the parser |
| BF-P2-08 | P2; ARCHITECTURAL DEBT; Latest/root resolver | Reproduced list/single/component disagreement when a lower version retained `isLatest=1`; several reducers were input-order dependent; equal package versions frozen under two plan versions could resolve to different rows; root-only and numeric/string root references were inconsistent | Plans, packages, contracts, partners, detail routes and selectors independently prioritized version, latest marker and related plan version in different orders; object-key grouping also left no stable final tie-break | Introduce a pure canonical resolver: normalized root identity, highest entity version, package-specific related-plan version, `isLatest` marker, then stable ID; expose list/single/root lookup, family sorting and per-version representative seams; migrate model getters, detail/router, plan/package/partner/contract/timeline components and plan snapshot selection while leaving version creation/deletion schema intact | `5d4cace` | Red order reversal, stale marker, root normalization/root-only lookup, duplicate package snapshot and list/single/component invariants; targeted version/navigation/delete/inheritance 34/34; full JS 603/603; security/Trusted Types, encoding and debt pass; modules 263/0 cycles; secure build 267 modules/48 bundles | N/A | FIXED | Explicit historical dropdown selections remain supported; corrupt duplicate rows now resolve deterministically, but this frontend resolver does not replace backend uniqueness/version invariants or rewrite the version model |
| BF-P2-09 | P2; PERFORMANCE/BUILD DEBT; CSS/bundle | Revalidated the current main CSS at 379,859 raw / 66,541 gzip / 52,725 Brotli: landing, legal and Assistant styles were forced through `app.css` for every path; landing/legal JS was also statically attached to the app entry despite mutually exclusive routes | The build plugin correctly enabled JS/CSS splitting, but the root stylesheet imported every feature and `app.js` statically imported public-route modules; Assistant CSS entered through global `components.css`, so Vite had no lazy CSS ownership seam | Dynamically import landing/legal modules by route; let each route await its content-hashed stylesheet before revealing the shell; load Assistant CSS only after enabled config at its existing lazy loader; allow only first-party source or hashed dist CSS URLs; add manifest ownership/hash/leak checks and a 330,000-byte main-CSS ratchet to secure build | `b198f66` | Main CSS 318,936 raw / 54,739 gzip / 43,822 Brotli (−60,923 / −11,802 / −8,903); route assets: landing 50,260, legal 11,227, Assistant 16,885 raw; 6 desktop/320px visual checks + real built-route runtime smoke pass with zero horizontal overflow; isolated built CSS navigation 30 cold p95 25.3 ms / 30 warm p95 35.6 ms; full JS 606/606; legal/startup transport 16/16; security/modules/debt/encoding pass; secure build 267 modules/52 bundles | N/A | FIXED | Large shared `views.css`/`ui-redesign.css` remain intentionally global until route ownership is characterized further; full authenticated startup/long-task attribution is BF-P2-10, and could not be rerun here because the pre-existing local backend's rate-limit DB was unavailable and correctly failed login closed |
| BF-P2-10 | P2; PERFORMANCE ISSUE; Startup | Baseline 30+30 log contained 26 `document-bootstrap` long tasks; cold runs 10 and 29 exceeded the 100 ms budget at 145/127 ms immediately before `app-module-start`, with host CPU 46.7%/69.4% and only two emitted fonts overlapping; Chromium attribution remained `window/unknown`, so no workflow/function was identified safely | Public-route CSS and app modules were still eager in the measured baseline, while the two outliers also correlated with substantial host contention; the captured attribution was insufficient to justify a speculative function refactor | Keep a deterministic profiler that reports phase, run, start, duration, bounded resource paths and host CPU without private payloads; preserve the reviewed 100 ms constant; after the route graph split, run the actual built landing app for 30 isolated cold + 30 warm navigations and enforce the long-task budget in the visual gate | `b198f66`, `7691d58` | Profiler regression 2/2 and baseline replay identifies exactly 2/26 over-budget tasks; built app: cold median 139.4 ms / p95 163.8 ms / longest 71 ms, warm median 117.8 ms / p95 152 ms / longest 67 ms; 6 desktop/320px visual checks; full JS 608/608; security/modules 263/0 cycles/debt/encoding pass | N/A | MITIGATED | The over-budget task is not reproducible on the current public built route and thresholds were not relaxed; authenticated 30+30 rerun remains unverified because the pre-existing local backend lost its persistent rate-limit database and correctly failed login closed with 429, so this is not claimed as an absolute fix |
| BF-P2-11 | P2; TEST FLAKINESS; Offline E2E | Audit run reproduced the interrupted-sync assertion observing `local-pending`, while two immediate reruns passed; the harness waited a fixed 250 ms after one partial controller predicate and assumed the 80 ms handoff had completed | The predicate covered only `_autoSyncPromise` and `_syncImmediateTimer`, omitting queued retry, deferred mutation, active workspace-mutation lifetime and pending outbox durability; elapsed wall time had no causal relation to the intercepted `/api/sync` failure, so a late workflow callback could overwrite the transient `transport-error` state | Export a bounded sync-activity snapshot that is settled only after promise/timer/queued/deferred/workspace-mutation work and outbox durability finish; arm a Playwright `requestfailed` barrier before submit, then require settled `transportError`, pending mutation and matching UI state; remove the 250 ms wait and duplicate assertion; add a fail-fast five-run isolated soak runner and make CI use it | `90266c3`, `6a6aa42` | Initial red sync-settlement contract failed on missing seam; removing the remaining sleep deterministically exposed `syncing` after the first barrier, and the follow-up red case proved an active workspace mutation was still considered settled; corrected real soak 5/5 with 25 sync attempts and exact row/server/reload assertions every run; full JS 616/616; security/modules 263/0 cycles/debt/encoding pass | N/A | FIXED | The canonical offline Chromium suite now uses network/state completion and repeat enforcement; unrelated E2E harness debt is BF-P2-12, and real Firefox/WebKit enforcement remains BF-P2-14 |
| BF-P2-12 | P2; TEST DEBT; E2E harness | Audit counted 29 arbitrary waits, four DOM `.click()` calls and 84 calendar literals; after intervening fixes, revalidation still found five `waitForTimeout` sites, four raw DOM clicks across offline/JV callbacks and raw years in six canonical business suites | Poll loops and sleeps approximated render/network completion; DOM clicks inside `evaluate` bypassed Playwright actionability; dates were copied from the audit month, while JV fixture master rows inherited current `created_at`, so the hardened effective-version resolver correctly rejected their older fixed opening/contract dates | Add one timezone-aware E2E test clock derived once per run and optionally frozen by `E2E_TEST_NOW`; migrate canonical JS calendar inputs and pass relative plan/package/master effective timestamps into the JV fixture; replace sleeps with locator/waitForFunction/network barriers or `expect.poll`; replace raw DOM clicks with check/uncheck/click locators; add an AST/source ratchet that rejects `waitForTimeout`, click-inside-evaluate and raw calendar years | `2747f15` | Red ratchet reported all five wait-bearing files, offline/JV raw clicks and six raw-year suites; clock red/green 2/2 and harness ratchet 2/2; Turnstile local matrix 6/6; corrected offline soak 5/5; multi-assignee pass; JV multi-lot targeted pass; full JV reached all 23 checkpoints and lifecycle all 34 checkpoints; CRUD 17 and pairwise 15 business checkpoints completed; full JS 616/616; Python quality, security/modules 263/0 cycles/debt/encoding pass | N/A | FIXED | CRUD/pairwise/full-JV/full-lifecycle currently exit nonzero only after their business assertions because the intended `/api/client-errors` backpressure returns 429 on a fresh DB; classification/telemetry handling remains BF-P2-20. Firefox/WebKit enforcement remains BF-P2-14 |
| BF-P2-13 | P2; TEST GAP; Coverage gates | Backend suite measured 45.75% line / 35.69% branch but enforced only a 28% global floor; 15 critical modules had stale low ratchets (for example websocket 12/2 against 31.6/21.6 actual), while 616 JS tests ran with no coverage collection or threshold | Python coverage policy was an old coarse baseline and critical floors were not advanced with new integration tests; CI invoked Node test directly without built-in coverage, so highly covered sync/persistence/security modules could regress while global test count stayed green | Raise Python global floor to 45% and all 15 per-module line/branch ratchets to reviewed evidence-backed levels; run Node 24 built-in coverage in package and CI with global 45% line / 60% branch / 60% function floors; parse the pinned reporter tree and enforce line/branch/function floors for 14 critical JS modules; preserve optional JUnit output in CI and add missing/below-floor plus no-lowering contract tests | `f201831` | Full Python 677/677, 45.75% line / 35.69% branch, 15/15 critical modules; full JS coverage 618/618, 46.38% line / 61.95% branch / 62.14% function, 14/14 critical modules; JS parser/gate 2/2 and Python coverage/CI contracts 7/7; Python quality, modules 263/0 cycles, encoding and diff checks pass | N/A | FIXED | Floors are regression ratchets, not a claim that every UI/controller module is well covered; low-coverage non-critical modules remain visible in the report and future test work should move thresholds only upward |
| BF-P2-14 | P2; TEST GAP; Cross-browser Playwright | Config declared Chromium, Firefox and WebKit, but CI installed only Chromium and ran custom Chromium scripts; the canonical contractor-violation spec was skipped without optional recorded fixture configuration | The declarative project matrix was never exercised by CI, while the only canonical spec depended on an optional contractor fixture and therefore could not provide a required zero-skip gate | Add a required fixture-free browser smoke spec; make contractor-violation discovery conditional on complete recorded fixture configuration; install and run Chromium, Firefox and WebKit in CI; document the enforced smoke matrix and the intentionally Chromium-only deep fixture-driven suites | `f232e9e` | Canonical Playwright smoke: Chromium 1/1 in 2.5 s, Firefox 1/1 in 4.9 s, WebKit 1/1 in 8.3 s, total 3 pass and 0 skip; CI/config contracts 6/6 | N/A | FIXED | Required cross-browser compatibility is now a real CI gate; deep business suites remain Chromium-only because their fixture harness is browser-specific, and that support boundary is explicit in `docs/e2e/BROWSER_MATRIX.md` |
| BF-P2-15 | P2; DEPENDENCY DEBT; Full npm lock | Revalidation reproduced one HIGH `GHSA-2v37-7h3g-55p8` in `nanoid@3.3.16` through Vite 8.1.4 → PostCSS 8.5.23; production-only audit remained clean | PostCSS permits `nanoid ^3.3.16`, but the lock retained the vulnerable 3.3.16 release; the ordinary dependency gate audited only production dependencies and the scheduled security workflow did not audit the npm lock | Refresh the compatible transitive lock to `nanoid@3.3.18`; add a reusable npm gate that audits both the complete lock and production subset; enforce it in full CI and as a weekly supply-chain job; add a contract regression for both enforcement paths | `5cb032c` | `npm ls nanoid` resolves Vite → PostCSS → `nanoid@3.3.18`; full `npm audit` 0 vulnerabilities; `npm audit --omit=dev` 0; `pip-audit` 0; CI/audit contracts 7/7; secure build 267 modules/52 bundles; Python quality and diff checks pass | N/A | FIXED | The affected package remains dev/build-only and is now pinned above the advisory range; registry advisory availability is an external dependency of audit time, while the immutable lock and weekly gate make recurrence visible |
| BF-P2-16 | P2; REPRODUCIBILITY DEBT; Python CI | Full, N+1 and startup workflows installed the editable project directly, allowing pip to resolve transitive dependencies on every run; the shipped hashed lock covered runtime only and omitted test extras plus the build backend | CI treated exact direct pins in `pyproject.toml` as a complete lock, while editable PEP 517 installation still resolved transitives and created an isolated build environment; no hashed test lock existed, and adding build inventory exposed vulnerable `setuptools@81.0.0` (`PYSEC-2026-3447`) | Generate `requirements-test.txt` with all test extras, build dependencies and hashes constrained to the runtime lock; include the hashed build backend in `requirements.txt` and update it to fixed `setuptools@83.0.0`; make all Python workflows install their appropriate lock with `--require-hashes`, then install the project using `--no-build-isolation --no-deps`; bind pip caches to the selected lock | `3945e3c` | Red CI contract failed on the resolving editable install; fresh Python 3.14 virtualenv installed the complete test lock by hash, built the editable project without dependency/build isolation resolution and passed 8/8 contracts; dependency/N+1 targeted 31/31; runtime and test-lock `pip-audit --disable-pip` both report 0 vulnerabilities; Python quality and diff checks pass | N/A | FIXED | Lock regeneration still depends on the configured package index, but each committed artifact records the exact Python 3.14 generation command and hashes; test dependencies are constrained to every runtime pin to prevent CI/deploy version drift |
| BF-P2-17 | P2; SBOM GAP; Vendored assets | The production npm SBOM contained only two registry components and the Python SBOM covered the runtime lock; eight packaged Flatpickr, Lucide, SheetJS and Plus Jakarta Sans files were absent despite having a pinned vendor manifest | The generator delegated only to npm and `cyclonedx-py`; neither package manager can discover checked-in third-party files, and the existing vendor audit was not connected to the CycloneDX dependency graph | Verify the vendor manifest and on-disk hashes during SBOM generation; add four required library components with version, SPDX license, distribution source and patch/security metadata; add eight file components with exact SHA-256, license and packaged path; link the application to vendor libraries and each library to its files using stable unique BOM references | `cc56492` | Red unit regressions failed on missing merge/inventory seams; targeted SBOM/dependency contracts 10/10; real generator run twice produced identical SHA-256 `2836c1c0ab41fe365e4cd976c1c40fc682d01a598acc79ed091deea21541139a` with CycloneDX 1.5, 4 vendor libraries and 8 vendor files; vendor audit verifies 8/8 pinned files; SheetJS/archive guards and Python quality pass | N/A | FIXED | CycloneDX now inventories every file declared by the authoritative vendor manifest; adding a new vendored asset still requires its reviewed source/version/license/hash metadata in that manifest, and generation fails closed if its file or checksum is stale |
| BF-P2-18 | P2; OBSERVABILITY GAP; Obfuscated client | Secure builds discarded both the bundler and obfuscator maps; client diagnostics retained only release/bundle/line/column, so an incident coordinate could not be traced through obfuscated chunks to reviewed source | Vite source maps and `javascript-obfuscator` source maps were both disabled, the post-build marker stored only hashes, and CI had no protected symbol artifact or tested offline symbolication path | Generate hidden Vite maps and separate obfuscation maps, strip every public `sourceMappingURL` and `.map`, then chain both maps in one content-addressed archive outside `dist`; bind it to the exact release ID and bundle hashes, refuse different-content overwrite for non-development releases, upload it as a separate 30-day CI artifact, add a fail-closed CLI/runbook, and run symbolication from the secure-build verifier | `e9ca558` | Red build contract failed on the missing direct map dependency/archive; synthetic two-map symbolication and path/release rejection 4/4; full JS coverage 621/621 with 14/14 critical ratchets; immutable-release secure build 267 modules/52 obfuscated bundles; archive 8,412,277 bytes with matching SHA-256 `f9895aa11ebf66dc3a299c326fbd2da5ad0f183436d4162d415766429352d13c`; real smoke resolves to `frontend/auth/AuthUi.js:101`; rebuild with the same release/archive is identical; public `dist` maps 0; package-path 5/5; security/modules/debt/encoding/dependency gates pass | N/A | FIXED | The archive contains private source content and is intentionally excluded from `dist` and the production ZIP; incident response depends on restricted CI-artifact access and the documented 30-day retention window, while local mutable `development` archives are explicitly unsuitable for production incidents |
| BF-P2-19 | P2; RELEASE DEBT; Release ID | A local secure build embedded `releaseId=development`, passed the secure artifact verifier and remained eligible for production file selection, making deployed client diagnostics impossible to correlate to one immutable build | The secure build intentionally permits a development fallback for local work, but the production packager validated only obfuscation flags/transformed files and neither classified the marker release nor compared it to the build environment | Add a production-boundary release validator requiring a full 40-character commit SHA or 64-character content hash; reject empty, whitespace, `development`, `unknown`, shortened and semantic-version IDs; require exact marker agreement with `APP_RELEASE_ID`, falling back to `GITHUB_SHA`; invoke the gate before runtime file selection and document the operator contract while preserving local development builds | `9d2c412` | Red release contract failed on the missing validator; 8 invalid/mutable vectors rejected, 40/64-character immutable IDs accepted, environment mismatch rejected; full production-package tests 16/16; real immutable secure marker selects 399 runtime files with no private symbols/source maps; Python quality, encoding and diff checks pass | N/A | FIXED | Local `npm run build:secure` may still use `development` by design, but no production archive can be selected from it; legal approval remains an independent intentional blocker and was not bypassed |
| BF-P2-20 | P2; OBSERVABILITY/QUALITY DEBT; Swallowed/caught errors | Revalidation preserved the aggregate BLE001=118/S608=128 counts but showed the gate could move findings between modules; critical sync/outbox/IndexedDB/Excel/WebSocket/startup catches emitted only generic counters or raw error objects, and deep E2E suites treated intentional `/api/client-errors` 429 backpressure as a business failure | The Python quality baseline tracked only repository totals, while frontend reporters accepted no bounded operational dimensions; startup emitted `Outbox.StartupRetry` after every successful initial push, creating needless telemetry on reload, and each deep E2E harness independently classified every API response >=400 as fatal | Add exact per-module BLE001/S608 ratchets; extend the allowlisted client-error schema with bounded operation/phase/retryability/status, 16-hex SHA-256 workspace scope and safe correlation ID; route only critical catches through structured reporters and remove raw caught objects from logs; emit startup retry only for a real post-pull replay; share an exact E2E classifier that ignores only POST `/api/client-errors` 429 while retaining every business API failure | `2d5faa7` | Red module-shift, structured-diagnostic, startup-retry and exact-classifier regressions; targeted JS 12/12 + Python 18/18; full JS coverage and 14/14 critical ratchets pass; Python quality BLE001=118/118 and S608=128/128; security/modules 263/0 cycles/debt/encoding/diff checks pass; real CRUD E2E completes 17 business checkpoints and fixture cleanup with the limiter unchanged | N/A | FIXED | Telemetry remains intentionally sampled/rate-limited and drops under backpressure; only reviewed critical boundaries were instrumented, raw workspace/error/prompt/token/document data is rejected, and remaining catch-count reduction is quality debt rather than an observability correctness blocker |
| BF-P2-21 | P2; SUPPLY-CHAIN RISK; CI refs | Revalidation found 20 external action uses across five workflows on mutable major tags, two PostgreSQL services on `postgres:17`, and the Gitleaks scanner on `v8.30.1`; the referenced `dependency-review-action@v5` major ref was absent upstream even though the reviewed `v5.0.0` release existed | Workflow dependencies trusted movable tag names, had no repository-wide immutable-ref gate, and had no configured review automation for action updates; an upstream retag or image rebuild could therefore change build/test/security code without a repository diff | Resolve reviewed official releases to full 40-character action commit SHAs with retained version comments; pin PostgreSQL 17.10 and Gitleaks 8.30.1 to registry multi-architecture SHA-256 digests; scan every workflow `uses:`, service image and registry image command in regression tests; add weekly Dependabot GitHub Actions PRs for reviewable SHA updates | `7856ca4` | Initial supply-chain gate failed all three contracts with 20 mutable action refs, 3 mutable images and no Dependabot config; targeted CI contracts 11/11; all workflow/dependabot YAML parses; upstream tag/SHA and multi-arch digest resolution verified against official GitHub repositories and registries; Python quality, encoding and diff checks pass | N/A | FIXED | Action updates are proposed as reviewed PRs; GitHub Dependabot does not support Docker Hub/GHCR workflow image URLs, so the two pinned image digests require explicit reviewed manual refresh, while the repository gate prevents reintroducing tag-only images |
| BF-P2-22 | P2; DATABASE ASSURANCE GAP; Schema contract | Revalidation confirmed startup compared exact names for 79 tables/1,116 columns but only a hand-picked subset of FK/index names; type, nullability, default, CHECK, PK/UNIQUE, full FK/index definitions, triggers, validity state and unexpected objects could drift undetected | The assurance logic mixed canonical table names with two manually maintained allowlists and never normalized PostgreSQL's deparsed catalog representation, so most semantic DDL had no expected value to compare | Generate a reviewed PostgreSQL 17 catalog snapshot from the canonical fresh schema; compare schema version, every table/column definition, all 779 constraints, all 379 index definitions/state and all 76 non-internal trigger definitions/state at startup; reject unexpected application tables/columns/constraints/indexes/triggers under an explicit policy; ship the snapshot in wheels and the production archive; add pure and rollback-only real-PostgreSQL negative drift tests | `e639aa3` | Initial red test failed on the absent normalized comparator; targeted schema/package/security 51/51; transactional real-PostgreSQL matrix rejects type/null/default/CHECK/UNIQUE/FK/index/trigger and extra table/index/trigger drift; live schema contract 79 tables, 1,116 columns, 779 constraints including 104 FK, 379 indexes and 76 triggers; full Python 732/732, coverage 45.96% + critical 15-module gate; FK audit 104/104; extracted production smoke 401 runtime files; Python quality, encoding and diff checks pass | N/A — no schema mutation; generated contract records current v43 catalog | FIXED | Contract normalization is intentionally pinned to PostgreSQL major 17; reviewed schema changes must regenerate the snapshot from a fresh schema. Extension-owned functions/types/sequences and internal PostgreSQL triggers remain explicitly outside the extra-object policy |
| BF-P2-23 | P2; MIGRATION RISK; Upgrade v36 | Revalidation confirmed historical v36 still fetches every lot/opening row, materializes both result sets and update lists in Python, performs per-row backfills and rebuilds two unique indexes inside one transaction | The migration predates production-scale cardinality controls and intentionally preserves exact Python NFKC/casefold semantics; rewriting it would invalidate reviewed migration history, while row count, relation size, collision behavior and lock/runtime budget were invisible to operators before execution | Keep v36 byte-for-byte unchanged; add read-only cardinality/size preflight for databases crossing v36; add `--dry-run` that executes the exact upgrade chain plus schema/FK validation then rolls back; exercise v36 on PostgreSQL with 3,000 lot + 6,000 opening rows; package a backup, maintenance/quiescence, timeout, collision-resolution and restore runbook | `45f9d7d` | Initial red suite failed on the absent preflight seam; unit/real-PostgreSQL/CLI targeted 16/16; realistic v36 test backfills 9,000 rows, preserves NOT NULL canonical values and replaces both expression indexes; live `--preflight`, transactional `--dry-run` rollback and ordinary v43 initialize pass; full Python 741/741, coverage 46.05% + critical 15-module gate; extracted production smoke 403 runtime files; Python quality, encoding and diff checks pass | N/A — historical v36 was not rewritten and no schema change was introduced | MITIGATED | v36 remains intrinsically O(N) in Python and holds DDL/write locks until commit; safety now depends on a production-like staging clone, verified backup, approved timeout/headroom and a quiesced maintenance window as required by the packaged runbook |
| BF-P2-24 | P2; DATABASE DEBT; `sync_metadata` invariants | Read-only revalidation found 53 live-development and 71 test rows with zero negative `current_version` and zero `min_available_version > current_version`; catalog enforced only `min_available_version >= 0`, so both impossible cursor states remained writable | Fresh schema and historical upgrades encoded only the lower bound for the retained cursor; no shared read-only violation count, migration preflight or database CHECK protected the current/retention ordering | Add append-only v44 with a tenant-ID-free data preflight; fail before DDL on either invalid count; add both CHECK constraints as `NOT VALID` then `VALIDATE CONSTRAINT`; expose the same counts through database `--preflight`; declare identical named constraints in the fresh schema and regenerate the normalized PostgreSQL 17 catalog | `7f5aee8` | Initial red suite failed 6/6 on missing v44/fresh constraints; migration/preflight/schema real-PostgreSQL matrix 14/14 and combined schema/startup targeted 49/49; temp-table validation covers 501 existing rows and rejects future negative/ahead inserts; live v44 catalog reports both constraints validated; full Python 748/748, coverage 46.11% + critical 15-module gate; fresh extracted production smoke 403 runtime files; schema contract 79 tables/781 constraints/379 indexes/76 triggers; FK audit 104/104; quality, encoding and diff checks pass | v44 `enforce_sync_metadata_version_bounds`; append-only, data preflight before DDL, two validated CHECK constraints | FIXED | A legacy database containing invalid rows intentionally cannot upgrade until an authorized, audited repair restores cursor semantics; the migration reports only aggregate counts and never guesses or silently rewrites tenant data |
| BF-P2-25 | P2; IDEMPOTENCY RISK; Lot finalize | Reproduced the lost-response boundary: the package update and artifact can commit, but a retry re-enters `finalize_batch_award`, checks the stale pre-commit `expectedVersion` before the closed-batch compatibility path and returns a conflict instead of the committed result | Finalize POST had no idempotency key, request identity or durable response receipt; the service's artifact content digest protected conflicting artifacts but could not replay the complete HTTP result, and the shared client therefore classified the POST as non-retryable | Require a validated finalize `Idempotency-Key`; generate a stable client key from the expected package version; hash the canonical outcomes/award request; serialize same-key commands with a transaction advisory lock; store digest plus complete success payload in existing `api_idempotency` inside the finalize/audit transaction; replay exact matches before stale-version checks and reject mismatched reuse with 409 | `c463c2d` | Initial red suite failed at import because no finalize idempotency contract existed; backend targeted 6/6 plus document/idempotency regression 13/13; simulated response-path failure after commit proves the second request replays while finalize, immutable audit and WebSocket broadcast each execute once; real PostgreSQL commit/reconnect replay passes and mismatched digest is rejected; frontend stable-key contract 3/3; full Python 754/754, coverage 46.28% + critical 15-module gate; full JS coverage passes at 46.55% line / 62.01% branch / 62.37% function with 14 critical modules; secure build 267 modules/52 bundles; security/modules/debt/quality/encoding/diff checks pass | N/A — reuses the existing durable `api_idempotency` table and retention lifecycle | FIXED | Replay is intentionally actor-, organization-, package-, batch-, key- and request-digest scoped and lasts for the configured API idempotency retention window (default seven days); retries after receipt expiry or by a different actor do not claim the original response |
| BF-P2-26 | P2; REALTIME CONSISTENCY RISK; WS outbox | Revalidation found the canonical sync/restore paths already inserted `websocket_events` with `pg_notify` on the caller transaction, but ten lot/org/auth/document/partner business broadcasts still committed first and then opened an independent outbox transaction; the compatibility broadcaster swallowed store failure, scheduled only local delivery and returned `True` | Durable broker primitives and retry/dead-letter state existed since v32, but most non-sync routes imported the convenience broadcaster instead of the transaction seam, so a process/database failure between business commit and enqueue could leave other replicas permanently stale while the caller was told publication succeeded | Route all reviewed lot-finalize, subscription/member/access, email-change, package-document and partner-enrichment invalidations through `enqueue_websocket_event` before the business commit; enqueue security revocations on the same relevant transactions; remove every direct business call to the post-commit broadcaster with an AST/caller ratchet; keep the compatibility fallback but log store failure and return `False` rather than claiming durability | `c5b5df1` | Initial red contracts showed legacy store failure returned true and business modules still called post-commit publication; outbox targeted 7/7 plus lot/document/N+1 regressions 38/38; real PostgreSQL proves event rows rollback and commit atomically with the caller transaction and remain `pending` for broker delivery; caller ratchet covers 17 transactional enqueue sites across eight reviewed modules and rejects direct business broadcaster calls; full Python 758/758, coverage 46.31% + critical 15-module gate; Python quality, encoding and diff checks pass | N/A — reuses existing `websocket_events` durable outbox, v32 retry/dead-letter state and commit-delivered PostgreSQL NOTIFY | FIXED | Delivery is intentionally at-least-once: clients must keep invalidation handling idempotent, broker failures retry up to the existing dead-letter policy, and operations must monitor pending/retry/dead-letter metrics. The legacy standalone broadcaster remains only as an explicit best-effort compatibility API and now reports `False` on durable-store failure |
| BF-P2-27 | P2; DEPLOYMENT SECURITY RISK; AI provider URL | Revalidation confirmed enabled hosted providers accepted arbitrary deployment-controlled base URLs, `urllib` followed redirects and ambient proxy variables automatically, legal search used a separate raw transport, and no startup gate bound credentials/tenant prompts to a reviewed destination | Provider configuration carried URL strings but no canonical destination policy; source citation allowlisting occurred only after the legal-search response and therefore could not protect outbound API keys/questions; transport delegated redirect/proxy behavior to global `urllib` defaults | Add one exact-host outbound policy with HTTPS/default-port/no-userinfo rules, canonical built-in OpenAI/Anthropic/Gemini and Azure resource hosts, explicit custom gateway allowlists and loopback-only Ollama; validate AI and web-search endpoint/proxy configuration before startup readiness and again before every request; replace raw transports with a no-redirect opener that ignores ambient proxies and accepts only an explicitly allowlisted HTTPS proxy; document all deployment variables and compatibility boundaries | `d0f05fb` | Initial red slices reproduced HTTP/custom-host/userinfo/non-default-port/Ollama/Azure/startup bypass, adapter request bypass, redirect target delivery and proxy-policy bypass; real loopback redirect server proves the changed host is never reached and ambient proxy discovery is not called; URL/transport targeted 54/54, all AI 106/106, AI+startup 130/130; full Python 787/787 at 46.71% with 15/15 critical coverage; full JS coverage pass at 46.53% line / 62.01% branch / 62.37% function with 14/14 critical modules; Ruff, Python quality, compileall and diff checks pass | N/A — configuration/transport hardening only | FIXED | DNS/TLS and an explicitly approved proxy remain operator trust boundaries; custom hosted endpoints now require an exact reviewed hostname, proxy use is opt-in and separately allowlisted, redirects fail closed, and Ollama remote endpoints are intentionally unsupported |
| BF-P2-28 | P2; PERFORMANCE RISK; Retention queries | Real PostgreSQL benchmark 200.000 rows/table với 5% hết hạn xác nhận owner-leading indexes buộc đọc/sort 10.000 rows cho tombstone/mutation và partner terminal cleanup dùng Seq Scan; cleanup cũ xóa toàn bộ bảy bảng trong một transaction | Global cutoff không khớp prefix index; `DELETE` không có giới hạn; transaction advisory lock sẽ mất ngay khi chia commit nên không thể batch an toàn giữa nhiều replica; tombstone watermark được tính toàn tập trước delete thay vì gắn với từng batch | Dùng CTE `ctid` + cutoff order + `LIMIT` + `FOR UPDATE SKIP LOCKED` cho cả bảy retained tables, batch cấu hình 1–10000/default 1000 và commit từng batch; giữ session advisory leader xuyên commit; `DELETE ... RETURNING` tombstone cập nhật từng organization watermark trong cùng transaction; thêm preflight/cardinality, runbook/rollback và ba cutoff-first/partial indexes chỉ sau planner evidence | `ac63e28` | Red cleanup test chứng minh cũ chỉ 1 unbounded commit; red planner thiếu v45; retention PostgreSQL 4/4, migration/preflight/schema targeted 54/54; 3/3 plan 200.000-row dùng đúng index; application + test DB preflight/dry-run/v45/catalog/FK 104/104 pass; full Python 792/792, coverage 46,84%, critical 15/15; full JS coverage 46,53% line / 62,00% branch / 62,37% function, critical 14/14; Ruff/quality/compile/diff pass | v45 `add_retention_cleanup_indexes`; append-only, ba index additive, transactional DDL; fresh schema/catalog và operator rollback contract cập nhật | FIXED | V45 dùng transactional `CREATE INDEX`, vì vậy production cần staging-equivalent EXPLAIN, backup và maintenance window; operator phải tune batch từ telemetry, còn session expiry predicate chưa có cutoff index riêng nhưng transaction đã bounded |
| BF-P2-29 | P2; MIGRATION TEST GAP; PostgreSQL upgrades | CI chỉ fresh final schema; không chạy real v1/vN→v42 chain | Revalidate at `ci.yml:67-71`; `postgres_schema.py:1082-1168`; `upgrades.py:1907-1937` | Fixture v1 khóa source/schema + đủ catalog index/trigger; v1/v35 real PostgreSQL chain; runner prerequisite registry cho historical assumptions; v46 append-only reconcile canonical catalog/data, fail closed và rollback an toàn | `5e91273` | `test_postgres_migration_chain` 6/6 với v1/v35, catalog/FK/data/invariant/public-schema isolation, exact-catalog no-rebuild và failed-conversion rollback; targeted 64/64; full Python 799/799, coverage 48.04% branch-aware + critical gate; full JS 46.52% line / 61.98% branch / 62.37% function, 14/14 critical; fixture/contract/Ruff/quality/compile/diff/FK audit pass | v46 `reconcile_historical_chain`; append-only, không sửa migration v1–v45, không drop table/column; preflight/runbook/CI rehearsal | FIXED | Historical v1/v35 installs now fail closed on unresolved catalog/data incompatibility; v46 rebuilds application constraints/indexes only when drift exists, so production vẫn cần backup/quiescence/dry-run trên PostgreSQL 17 cardinality tương đương |
| BF-P2-30 | P2; PRIVACY/RETENTION RISK; Account deletion | Real PostgreSQL xác nhận account deletion cũ trả 200 dù `personal:<user_id>` còn tombstone chứa `record_snapshot_json`; interleaving còn cho sync đã xác thực trước chờ deletion lock rồi tiếp tục ghi sau khi user bị xóa | Account deletion chỉ đếm bảng nghiệp vụ có `owner_type`, bỏ sót retained tombstone không FK user; chưa có retention/erasure/legal-hold rule authoritative; personal lock ban đầu được lấy sau transaction-time authorization và bootstrap `sync_metadata` không dùng cùng boundary | Fail closed 409 `ACCOUNT_DELETION_RETENTION_REVIEW_REQUIRED` với aggregate `personalTombstones` duy nhất, không lộ snapshot và không purge audit evidence; dùng transaction advisory lock chung cho account deletion, sync, restore và personal metadata bootstrap, lấy lock trước bước reauthorization để stale request không ghi sau xóa; thêm runbook nêu rõ các quyết định retention/legal/product bắt buộc | `570b319` | Red deletion regression và red sync-vs-delete interleaving; account/lock/runbook PostgreSQL 5/5; auth/sync/restore/word/last-admin targeted 15 pass, 2 environment skips; full Python 804/804, coverage 48,17% branch-aware + critical gate; full JS 46,52% line / 62,00% branch / 62,37% function, 14/14 critical; secure build 267 modules/52 bundles; Ruff/quality/compile/encoding/diff pass | N/A — không thay schema, không thêm cascade/FK và không purge/anonymize retained evidence | BLOCKED BY EXTERNAL/LEGAL/PRODUCT DECISION | Kỹ thuật đã ngăn xóa/lộ ngoài ý muốn nhưng không tự quyết lawful basis, retention duration, legal hold, anonymization, export/erasure ordering hoặc exception owner; deletion tiếp tục fail closed cho tới khi các owner có thẩm quyền phê duyệt workflow |
| BF-P2-31 | P2; DATA INTEGRITY RISK; Org decommission | Schema-derived revalidation xác nhận đúng 62 bảng có `organization_id`, 39 bảng có `owner_type`; production backend có 0 `DELETE FROM to_chuc`, còn direct deletes chỉ nằm trong test/E2E fixture cleanup | Organization decommission chưa là product feature nên không có transaction/invariant boundary, ownership registry, retention decision hoặc postcondition; chỉ membership/subscription FK trực tiếp tới root và mô hình polymorphic khiến cascade/FK hàng loạt không an toàn | Sinh canonical ownership registry trực tiếp từ schema; cung cấp inspection chỉ trả count và postcondition fail closed với retained-table allowlist explicit; thêm CI ratchet cấm direct organization delete trong backend và runbook chặn console/SQL/cascade cho tới khi có workflow được phê duyệt | `373e0ac` | Registry/dry-run/postcondition/backend-ratchet/runbook 6/6; exact 62/39 drift assertions; full Python 810/810, coverage 48,22% branch-aware + 15/15 critical; full JS gate gần nhất 46,52% line / 62,00% branch / 62,37% function, 14/14 critical; Ruff/quality/compile/encoding/diff pass | N/A — không thêm endpoint, migration, FK/cascade hoặc destructive SQL | MITIGATED | Runtime path hiện được chặn và primitive chỉ read-only; DBA vẫn có thể bypass bằng SQL ngoài ứng dụng. Feature tương lai phải có product lifecycle, retention/legal hold, writer quiescence, transaction/resumable state machine, artifact cleanup và real-PostgreSQL recovery tests trước khi bỏ ratchet |
| BF-P3-01 | P3; LIFECYCLE SMELL; NotificationCenter | Revalidation xác nhận shell hiện bootstrap một lần và init guard ngăn duplicate, nhưng component giữ một interval, ba document listeners, ba element listeners và in-flight refresh mà không có teardown/remount contract | Anonymous handlers và interval ID không được retained; init guard không được reset và active workspace request không có component-owned abort path | Retain named handlers, interval ID và active request; expose idempotent `dispose()` để abort refresh, clear interval, remove đủ sáu listeners, đóng panel, reset init guard/controller reference và cho phép remount; async handlers không commit UI sau dispose | `7a0261b` | Red lifecycle regression thiếu `dispose`; notification/workspace/startup/WebSocket targeted 19/19; full JS coverage 46,55% line / 61,99% branch / 62,42% function, 14/14 critical; module graph 263/0 cycles; ESLint/debt/diff pass | N/A | FIXED | Current shell vẫn one-shot nên không cần teardown caller; mọi lifecycle remount/logout-without-navigation tương lai phải gọi API `dispose()` trước khi thay DOM/controller |
| BF-P3-02 | P3; CONFIRMED DEAD CODE; Award direct/special branch | Source/caller characterization xác nhận direct/special approval gọi `saveKetQuaChiDinhThau` rồi return trước command/workflow; sole workflow caller còn lại và toàn bộ tests truyền `isDirectOrSpecial=false` | Shared workflow vẫn giữ nhánh legacy tạo contractor/opening giả dù UI direct/special đã được tách sang application command riêng; không có active caller hay compatibility contract cho nhánh này | Giữ nguyên legacy application command và toàn bộ active shared award workflow; xóa riêng helper/import/123-line branch không thể tới; shared workflow fail closed nếu direct/special command bị route nhầm; khóa source routing + no-mutation/no-port regression | `6ac6278` | Initial guard regression đi vào dead branch và lỗi `replaceTableState`; award/qualified targeted 7/7; full JS coverage 46,64% line / 62,07% branch / 62,53% function, 14/14 critical; secure build 267 modules/52 bundles; ESLint/modules/debt/diff pass | N/A | FIXED | Direct/special award vẫn là active legacy app-command flow và không bị xóa; nếu hợp nhất workflow tương lai phải migration/characterization riêng thay vì bỏ fail-closed guard |
| BF-P3-03 | P3; CONFIRMED DEAD CODE; Evaluation metadata aliases | Repo-wide exact-symbol scan xác nhận `parseEvaluationMetadata`, `parseStrict`, `parseForDisplay`, `serialize`, `migrate` chỉ xuất hiện tại declarations; mọi caller dùng sáu tên canonical | Convenience aliases được export khi codec được harden nhưng không có import, dynamic registry, test contract hoặc public package consumer | Xóa đúng năm alias; giữ nguyên schema constants cùng migrate/strict/display/serialize canonical functions; add exact module export-surface regression để alias không quay lại | `165644d` | Red export-surface test liệt kê đủ 5 symbol thừa; codec/persistence targeted 8/8; full JS coverage 46,64% line / 62,07% branch / 62,53% function, 14/14 critical; secure build 267 modules/52 bundles; ESLint/modules/debt/diff pass | N/A | FIXED | Module là private frontend source, không phải published API; canonical exports vẫn giữ nguyên behavior, strict size/schema validation và display recovery contract |
| BF-P3-04 | P3; BROKEN TOOLING/DOCS; Business matrix | `npm run test:e2e:business-matrix` reproduced `MODULE_NOT_FOUND`; history confirmed generator and all `docs/e2e` were deliberately removed together, while package script was left behind | Removed generator produced 624 `NOT_RUN_AS_EXACT_TUPLE` rows and referenced deleted `e2e/specs/auth/auth.smoke.spec.mjs`; restoring it would recreate stale coverage claims rather than executable evidence | Intentionally retire the orphan npm command, matching the already-retired docs/generator; add generic package-script contract that resolves every direct local Node entrypoint and fails on any missing `.js/.mjs/.cjs` path | `9152ed2` | Red contract reported only `test:e2e:business-matrix:e2e/generators/generate_business_matrix.mjs` after wildcard exclusion; package-path/startup targeted 5/5; full JS gate immediately prior 46,64% line / 62,07% branch / 62,53% function, 14/14 critical; ESLint/diff pass | N/A | FIXED | No exact 624-tuple execution claim remains. Active Playwright browser/contractor specs and dedicated lifecycle/pairwise E2E commands remain separate; a future matrix must map tuples to real executable evidence before adding a command/docs back |
| BF-P3-05 | P3; CI MAINTAINABILITY DEBT; Duplicate/drifting gates | CI revalidation xác nhận manual steps bỏ `lint:encoding`/`lint:modules`, chạy security ở step riêng rồi lặp trong secure build, và `package:production` build lại bundle đã verify | Package scripts và workflow cùng tự orchestration một phần quality matrix; report-specific pytest flags khiến CI không reuse local gate, còn packaging không có explicit from-verified-build path | Add reusable `check:static` gồm compile/schema/historical fixture/Python/encoding/modules/debt; `check:ci` chạy static + canonical Python/JS tests + secure build một lần, với JUnit/coverage XML qua env; CI gọi duy nhất gate này và package bằng `package:production:from-build` vẫn giữ legal production check | `774999b` | Red CI contract thiếu canonical step; dependency/package/build-path targeted 29/29; direct `npm run check:static` pass; prior full Python 810/810 and JS 46,64%/62,07%/62,53% with critical gates; secure build 267 modules/52 bundles; fixture/schema/quality/encoding/modules/debt/diff pass | N/A | FIXED | PostgreSQL setup, artifact upload, FK/package/SBOM, E2E và dependency audit vẫn là CI-only operational steps; canonical package gate owns shared static/test/build coverage without weakening legal or artifact checks |
| BF-P3-06 | P3; POSSIBLY UNUSED; Benchmarks | Both standalone scripts executed successfully: persistence reports 100/1.000/10.000-row whole-table vs explicit-change medians; rollback-only PostgreSQL matrix at size 1/10 reports fixed query counts 3/7/1/2/2 and zero repeated patterns | Scripts were valid manual diagnostics but absent from package registry/docs, so ownership, invocation, safety boundary and whether timing was a release gate were undiscoverable | Keep both scripts; register `benchmark:persistence` and `benchmark:n-plus-one`; document maintainer ownership, intended change surfaces, TEST_DATABASE_URL/rollback guarantee, query-count-vs-timing interpretation and explicit non-CI/non-SLO status; add discoverability/path contract | `92ab1c4` | Both npm benchmark commands pass; persistence explicit path measured >26x synthetic speedup at all sizes; N+1 size 1/10 has zero repeated SQL patterns and rollback; package/docs contract 2/2; Ruff/ESLint/diff pass | N/A | VERIFIED RESOLVED | Benchmarks remain manual because host timing is noisy and no production telemetry/SLO was inferred; correctness/query-count regressions stay in automated tests, and maintainers must attach comparable before/after output when using these diagnostics |
| BF-P3-07 | P3; DATABASE DEBT; Duplicate audit index | Catalog v46 authoritative có 382 indexes và xác nhận hai unique btree exact `(chain_id, previous_hash)`: constraint-backed `audit_log_chain_id_previous_hash_key` và explicit `idx_audit_log_single_successor`; snapshot audit ghi nhận mỗi twin 1,104 kB và 0 scans | Fresh schema khai báo explicit index dù `SCHEMA_DINH_NGHIA.audit_log` đã có `UNIQUE(chain_id, previous_hash)`; không có preflight bảo đảm tên index vẫn trỏ đúng exact twin trước khi drop | Thêm append-only v47 với catalog preflight so sánh table/key/opclass/collation/options/expression/predicate/access method và fail closed nếu constraint twin thiếu hoặc explicit khác semantics; chỉ `DROP INDEX IF EXISTS idx_audit_log_single_successor`; giữ constraint-backed uniqueness, xóa declaration fresh, regenerate PostgreSQL 17 contract và document preflight/dry-run/backup/backward compatibility/rollback | `dbc432fb` | Red v46→latest regression giữ cả hai twin; v47 real PostgreSQL 4/4 gồm installed-v46, fresh-v47, duplicate successor rejection và non-exact named-index fail-closed; migration/preflight/catalog/FK targeted 49/49; app + test DB preflight/dry-run/upgrade/check pass; final catalog 79 tables, 381 indexes, 76 triggers, FK 104/104; v1 fixture source unchanged; Ruff/mojibake/diff pass | v47 `drop_duplicate_audit_successor_index`; append-only, không sửa v1–v46, không drop table/column/constraint/data; rollback tạo lại đúng explicit unique index theo runbook | FIXED | `DROP INDEX` vẫn cần maintenance window ngắn để tránh lock wait trên audit writers; rollback có chủ đích tái tạo double-write/storage overhead và chỉ được dùng cùng catalog validation, còn uniqueness luôn do constraint-backed twin giữ |
| BF-P3-08 | P3; CONFIRMED DEAD/LEGACY CHAIN; Timeline DOCX | Runtime characterization xác nhận public `/api/export-timeline/{package_id}` và frontend đều dùng Excel; `export_routes.export_timeline_api` dispatch `routes_excel`, còn DOCX handler chỉ tồn tại trực tiếp trong `routes_docx` mà không có route caller | Lần chuyển timeline sang Excel trước đây không retire đồng bộ DOCX handler, `render_timeline_docx` worker/IPC operation, generated system template, startup provisioning, template-management exceptions, legacy renderer và tests của chain cũ | Giữ nguyên tenant-scoped `timeline_context_service`, Excel workbook/worker, snapshot check, entitlement và audit path active; xóa riêng orphan DOCX route/import, IPC allowlist/worker branch, startup/system-template provisioning, special template handling, toàn `timeline_document_service.py` và legacy renderer tests; thêm retirement contract khóa dispatcher Excel và absence của bốn legacy surfaces | `95a10cc` | Red retirement regression thấy `routes_docx.export_timeline_api`; retirement 2/2 xanh; document/timeline/startup/Word targeted 79/79; active Excel route/context/worker tests pass; repo production scan không còn `timeline_document_service`, `mau_timeline_goi_thau` hoặc `render_timeline_docx`; compile/Ruff/mojibake/diff pass; Python BLE001 ratchet giảm 118→117 và module route 9→8 | N/A | FIXED | Không tự xóa file template mutable đã tồn tại trên volume của operator; chúng không còn được provision/protect hay dùng bởi timeline route và có thể được quản lý như artifact thông thường. Active document worker, generic Word renderer và timeline Excel remain intact |
| BF-P3-09 | P3; TEST ISOLATION BUG; LP-25 fixture | Real PostgreSQL red test seed đúng 7 mutable business rows rồi cleanup cũ vẫn còn đủ 7; source audit trước đó quan sát 7 runs × 7 = 49 rows | Cleanup cũ xóa root organization/accounts nhưng owner-scoped business tables không có root FK cascade; lần sửa đầu còn phát hiện xóa `deleted_records` trước business rows khiến tombstone triggers tự tạo lại 5 rows; fixture cũng không chặn development/production database | Yêu cầu `APP_ENV=test` và database name thuộc built-in CI/test hoặc explicit `LP25_DATABASE_ALLOWLIST`; xóa child/support/business theo reverse dependency, xóa tombstone sau business, sessions/membership/root/accounts sau cùng; hậu kiểm tất cả mutable fixture tables bằng zero-row postcondition; E2E assert `remainingRows=0`; giữ immutable `audit_log` theo retention contract thay vì bypass trigger | `20a861b` | Initial red 7→7 residue; intermediate regression bắt 5 tombstone tạo lại; PostgreSQL fixture/guard 4/4; full LP-25 browser E2E pass với stale 409, offline validation reject, `deletedRows=23`, `remainingRows=0`, `retainedAuditRows=4`; read-only app/test DB scan không còn mutable `lp25-*` rows; targeted quality/dependency 14/14, Ruff/S608 128/128, JS syntax/mojibake/diff pass | N/A | FIXED | Audit chain rows của test run được giữ có chủ đích vì `AUDIT_LOG_IMMUTABLE` và retention/integrity contract; local E2E trên database tên ngoài built-in allowlist phải explicit opt-in, nhưng vẫn không thể chạy nếu `APP_ENV` khác `test` |

## Execution log

- 2026-08-09: fetched `origin`; `main == origin/main == 47629c307...`; captured baseline above.
- 2026-08-09: production legal/package gates remain intentionally blocked; no approval was fabricated and no gate was bypassed.
- 2026-08-09: completed all code-owned P0/P1 remediation; 16 findings are `FIXED` and BF-P1-07 remains correctly blocked by missing authoritative legal/product approval. P2/P3 remain in progress.
- 2026-08-09: completed BF-P2-28 with v45 cutoff indexes proven at 200.000-row cardinality, bounded multi-transaction cleanup and a session-scoped single-leader lock; master status is 42 `FIXED`, 2 `MITIGATED`, 1 legal blocker and 12 pending findings.
- 2026-08-09: completed BF-P2-29 with source-pinned v1 catalog fixtures, real v1/v35 PostgreSQL chain rehearsal, v46 drift reconciliation and rollback/no-rebuild assertions; master status is 43 `FIXED`, 2 `MITIGATED`, 1 legal blocker and 11 pending findings.
- 2026-08-09: hardened BF-P2-30 with fail-closed personal tombstone detection, post-lock account reauthorization and a no-purge retention runbook; the remaining policy is intentionally blocked on authoritative privacy/legal/product approval. Master status is 43 `FIXED`, 2 `MITIGATED`, 2 external/legal/product blockers and 10 pending findings.
- 2026-08-09: mitigated BF-P2-31 while organization decommission remains unsupported: schema-derived ownership inventory covers 62 scoped/39 polymorphic tables, postconditions fail closed and CI rejects a direct backend root delete. Master status is 43 `FIXED`, 3 `MITIGATED`, 2 external/legal/product blockers and 9 pending findings.
- 2026-08-09: fixed BF-P3-01 with an idempotent notification-center disposal/remount contract covering interval, DOM listeners and in-flight workspace requests. Master status is 44 `FIXED`, 3 `MITIGATED`, 2 external/legal/product blockers and 8 pending findings.
- 2026-08-09: fixed BF-P3-02/03 by characterizing and removing only the unreachable direct-award workflow branch plus five unused evaluation-metadata aliases; active legacy direct award and canonical codec APIs remain intact. Master status is 46 `FIXED`, 3 `MITIGATED`, 2 external/legal/product blockers and 6 pending findings.
- 2026-08-09: fixed BF-P3-04 by retiring the stale business-matrix command together with its already-removed non-executable claims and adding a generic local Node entrypoint path gate. Master status is 47 `FIXED`, 3 `MITIGATED`, 2 external/legal/product blockers and 5 pending findings.
- 2026-08-09: fixed BF-P3-05 by consolidating shared static/test/secure-build checks behind `npm run check:ci`, restoring encoding/module gates and packaging from the already verified build without bypassing legal readiness. Master status is 48 `FIXED`, 3 `MITIGATED`, 2 external/legal/product blockers and 4 pending findings.
- 2026-08-09: verified BF-P3-06 as retained manual diagnostics rather than dead code; both benchmarks now have package entrypoints, ownership/safety documentation and runnable evidence. Master status is 48 `FIXED`, 3 `MITIGATED`, 1 `VERIFIED RESOLVED`, 2 external/legal/product blockers and 3 pending findings.
- 2026-08-10: fixed BF-P3-07 with append-only v47: the migration fails closed unless the named index is the exact twin, drops only the explicit duplicate and preserves constraint-backed audit successor uniqueness. Master status is 49 `FIXED`, 3 `MITIGATED`, 1 `VERIFIED RESOLVED`, 2 external/legal/product blockers and 2 pending findings.
- 2026-08-10: fixed BF-P3-08 by retiring only the unregistered timeline DOCX route/IPC/template chain while keeping the public Excel route, tenant-scoped context and active document worker. Master status is 50 `FIXED`, 3 `MITIGATED`, 1 `VERIFIED RESOLVED`, 2 external/legal/product blockers and 1 pending finding.
- 2026-08-10: fixed BF-P3-09 with guarded reverse-dependency cleanup, tombstone-last ordering and a PostgreSQL zero-row postcondition; immutable audit evidence remains intentionally retained. Master status is 51 `FIXED`, 3 `MITIGATED`, 1 `VERIFIED RESOLVED`, 2 external/legal/product blockers and 0 pending findings.

## Final acceptance

1. **Latest HEAD ban đầu?** `47629c307b1ab6289918eb638fdddf09488a7639`, bằng `origin/main` sau `git fetch origin` ngày 2026-08-09. Technical acceptance được chạy trên chuỗi remediation đến `101245b`; sau đó `origin/main` tiến thêm commit tài liệu audit-only `f93c17b`, không đổi runtime/schema/test implementation.

2. **Bao nhiêu P0/P1/P2/P3 FIXED?** 51 `FIXED`: P0 3/3, P1 13/14, P2 27/31, P3 8/9. Không còn P0 active; P1 còn một external legal blocker, P2 còn ba mitigations và một external legal/product blocker, P3 còn một verified-resolved item.

3. **Bao nhiêu MITIGATED?** 3, đều ở P2: BF-P2-10 startup attribution, BF-P2-12 E2E timing debt và BF-P2-23 historical v36 migration risk. Các guard/profiler/barrier/runbook tương ứng đã được triển khai; residual risk được giữ rõ trong từng row, không đổi thành `FIXED` khi evidence chưa đủ.

4. **Bao nhiêu VERIFIED RESOLVED?** 1: BF-P3-06. Hai benchmark là manual diagnostics hợp lệ, có package entrypoint, ownership/safety documentation và runnable evidence; không bị xóa như dead code.

5. **Bao nhiêu BLOCKED và vì sao?** 2. BF-P1-07 bị chặn bởi 27 legal facts chưa được phê duyệt và 27 public placeholders chưa được thay. BF-P2-30 bị chặn bởi quyết định authoritative về lawful basis, retention duration, legal hold, anonymization và erasure/export ordering; kỹ thuật đã fail closed account deletion thay vì purge hoặc lộ audit evidence.

6. **Còn cross-tenant path đã biết không?** Không còn known active cross-tenant path trong các seams đã audit. Cross-org AI role/search/analytics, workspace switch, organization membership, Word access, document/media access và negative tenant assertions đều pass. Kết luận này bị giới hạn ở graph/routes và test matrix đã audit, không phải tuyên bố tuyệt đối cho external code chưa quan sát.

7. **Còn path mất dữ liệu đã biết không?** Không còn known active data-loss path trong scope audit. Pending local mutations được overlay trước pull; pull cursor serialized; direct persistence có checkpoint/rollback; workspace A→B async/mutation leases pass; stale/offline conflict trả 409 hoặc giữ durable outbox. Account deletion có unresolved retention decision nhưng hiện fail closed, không xóa mù.

8. **Word access enforce thật chưa?** Có. Stored restriction flags được enforce xuyên API/context/export, gồm all-false, từng flag, mixed, manager/employee/organization và negative DOCX assertions. Runtime không còn `allow_all()` trái với stored policy; active document worker và Word mapping/template paths vẫn được giữ.

9. **Personal workspace realtime fallback thật chưa?** Có. Personal workspace luôn giữ polling khi WebSocket chưa usable; close 4003/auth rejection, reconnect, BFCache, two-tab, logout và workspace switch đều có regression. Polling chỉ dừng khi WS `ready` và resume khi reconnect/unusable.

10. **Stale delete còn thắng update mới không?** Không trong paths đã audit. Hard delete/archive dùng expected `row_version`, rowcount exactly one và savepoint rollback; real PostgreSQL two-connection barriers giữ v2, trả `ROW_VERSION_CONFLICT` và không tạo tombstone/audit giả khi CAS thua.

11. **Migration mới nào?** Năm append-only migrations sau baseline v42: v43 `bind_session_active_role_to_workspace`; v44 `enforce_sync_metadata_version_bounds`; v45 `add_retention_cleanup_indexes`; v46 `reconcile_historical_chain`; v47 `drop_duplicate_audit_successor_index`. Không sửa v2-v42, không drop table/column.

12. **Dead code nào đã xóa?** Xóa 5 confirmed-unused `evaluationMetadata` aliases; nhánh shared award direct/special không thể tới; timeline DOCX legacy route/IPC/template chain. Active direct/special application command, timeline Excel/context, document worker và unknown modules/benchmarks được giữ. Stale business-matrix command/docs được retire có path gate thay thế.

13. **DB object nào đã xóa và rollback?** Chỉ explicit duplicate unique index `idx_audit_log_single_successor` qua v47. Constraint-backed twin `audit_log_chain_id_previous_hash_key` vẫn enforce `(chain_id, previous_hash)`. Preflight fail closed nếu hai index không còn exact twins; rollback runbook tái tạo đúng explicit unique index, chấp nhận quay lại double-write/storage overhead.

14. **Test counts trước/sau?** Python 633/633 → 817/817; JavaScript 522/522 → 641/641. Final Python branch-aware coverage 47.98% và critical gate pass. Final JS coverage 46.64% line, 62.07% branch, 62.53% function; 14/14 critical modules pass. Python quality giữ BLE001 117/117, S608 128/128, F401/F841/S110 0; frontend graph 263 modules, 0 cycles; debt ratchets không regression.

15. **E2E/browser/soak?** Pass: auth shell; 14-step auth/RBAC/cross-workspace/session suite; CRUD; bidder goods; multi-assignee; JV multi-lot/two-envelope/Excel/Word/contract; LP-25 stale/offline conflict; offline/reconnect; pairwise 15 packages; full lifecycle; UI quality; authenticated UI matrix. Offline soak pass 5/5. Browser matrix dùng isolated manager fixture pass Chromium 5.1 s, Firefox 10.5 s, WebKit 11.8 s và cleanup 6 accounts/3 organizations; raw shared-admin attempts correctly exposed single-active-session contention and không được dùng làm pass evidence. Workspace switch/pull ordering/personal fallback/JV multi-lot soak coverage còn nằm trong deterministic JS, auth và deep-E2E regressions tương ứng.

16. **Performance trước/sau?** Baseline: cold p95 539 ms, warm p95 150 ms, longest task 145 ms nên fail 100 ms budget. Final secure artifact 30 cold + 30 warm: cold median/p95 363/402 ms, warm median/p95 136/174 ms, longest task 85 ms, 0 runtime failure; pass budgets 800/325/100. Hai diagnostic runs với raw `APP_DEBUG=True` modules dao động và fail 825 ms cold hoặc 361 ms warm; chúng không được dùng thay cho secure-artifact release measurement.

17. **Dependency/SBOM/release status?** `npm audit`, `npm audit --omit=dev` và `pip-audit` đều 0 known vulnerabilities. Vendored audit, SheetJS/archive guards pass. CycloneDX dependency + vendored-asset SBOMs generated. Secure build pass 267 modules/52 obfuscated bundles; production package/extracted smoke pass 409 runtime files, 2,471,403 bytes với immutable full-SHA release marker.

18. **Legal gate pass hay blocked?** Development warning gate exit 0; production-public gate intentionally exit 1 với `LEGAL_FACT_UNAPPROVED=27` và `LEGAL_PLACEHOLDER_PRESENT=27`. Không có bypass hoặc fabricated approval. Production public release vẫn blocked.

19. **Có finding nào chưa có disposition?** Không. Đủ 57/57 rows: 51 `FIXED`, 3 `MITIGATED`, 1 `VERIFIED RESOLVED`, 2 `BLOCKED BY EXTERNAL/LEGAL/PRODUCT DECISION`, 0 `PENDING`, 0 `DEFERRED`.

20. **Có đủ điều kiện technical release không?** Phần technical remediation/package có đủ evidence trong scope audit: canonical `npm run check`, schema/FK 104/104, real upgrade/concurrency regressions, security-deploy 73/73 + Turnstile matrix, dependency audits, secure performance và isolated browser/deep-E2E đều pass. Tuy nhiên không được gọi production release-ready: BF-P1-07 legal approval chưa pass và BF-P2-30 retention/product workflow chưa có quyết định authoritative. Kết luận chính xác là: technical remediation complete where possible, but production release remains blocked by legal approval; account deletion tiếp tục fail closed pending retention decision.
