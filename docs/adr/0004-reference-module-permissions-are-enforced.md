---
status: accepted
---

# Investor and contractor module permissions are enforced

The exposed `chudautu` and `nhathau` permission-matrix values control base record read and write access for ordinary organization members. Sensitive financial, identity, signature, stamp, and private-media access remains a separate capability and is never implied by base module visibility; managers, personal workspace owners, and platform administrators retain only the explicitly documented inherited behavior.
