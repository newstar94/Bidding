---
status: accepted
---

# Contract-package links retain exact historical evidence

`hop_dong_goi_thau` remains linked to the exact package snapshot used when the contract was recorded so historical evidence is not rewritten by later versioning. Consumers asking about the logical current package resolve contracts through the tenant-scoped package lineage and explicitly label the relation as lineage-derived; links are not copied blindly to every new snapshot.
