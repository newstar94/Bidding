---
status: accepted
---

# Export operations have three classifications

Every export operation is registered as an empty template, a local draft export, or an official/compliance export. Official exports use a committed server snapshot with current server-side authorization, entitlement, and audit; local draft exports are visibly marked non-official, while free empty templates require an explicit server-owned allowlist rather than inheriting access accidentally.
