# BiddingFlow

BiddingFlow is the procurement package and bid-evaluation workspace.

The production bundle is built with `npm run build:plain` (or the secure
production build) and initialized against PostgreSQL with:

```text
python scripts/manage_database.py
```
